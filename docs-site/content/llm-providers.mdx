# AI Models

Bring your own model keys and every agent, Model Call node, and Model Prompt tool can use them. almyty supports every provider in the table below out of the box, including local models via Ollama, plus any OpenAI-compatible endpoint — so you are never locked to one vendor.

![Models page](/screenshots/models.png)

## Add a provider in the UI

1. Open **Models** in the sidebar and click **Add Provider**.

![Add Provider dialog](/screenshots/llm-providers-create.png)

2. Select the provider type.
3. Enter a display name and your API key.
4. Choose a default model from the dropdown.
5. Optionally adjust temperature, max tokens, and top-p.
6. Click **Test Connection**. almyty sends a lightweight request to verify the key and model.
7. Click **Save**.

The provider now appears in the Models list, ready for Model Call nodes and Model Prompt tools.

> **Pick from the live model list.** The dropdown is populated from the
> provider's current models, not a hardcoded list. Providers deprecate models
> quickly, so prefer the live list over pasting a model string by hand: a name
> that worked last quarter may already 404.

## From your AI assistant

The native MCP control plane exposes `add_provider` and `list_providers`, so you can wire up a provider from Claude Code or Cursor without opening the dashboard. See [Control almyty via MCP](/control-via-mcp).

## Supported providers

{/* generated:providers start */}

almyty speaks to 33 providers out of the box, plus any
OpenAI-compatible endpoint you point it at.

| Provider | Type | API format | Serves |
|----------|------|------------|--------|
| Anthropic | `anthropic` | Anthropic messages (preferred) | Claude Opus, Sonnet and Haiku |
| AWS Bedrock | `aws_bedrock` | OpenAI chat completions (preferred) | Bedrock models in your own AWS account |
| Azure AI Foundry | `azure_ai_foundry` | OpenAI chat completions (preferred) | Foundry model deployments in your own Azure subscription |
| Baidu ERNIE (Qianfan) | `qianfan` | OpenAI chat completions (preferred) | ERNIE 5.1 and the open models Qianfan hosts |
| Baseten | `baseten` | OpenAI chat completions (preferred) | Open models, and your own weights or container |
| ByteDance Doubao (Ark) | `volcengine` | OpenAI chat completions (preferred) | Doubao Seed 2.0, and DeepSeek on Ark |
| Cerebras | `cerebras` | OpenAI chat completions (preferred) | Open models on wafer-scale hardware |
| Cohere | `cohere` | OpenAI chat completions (preferred) | Command and the Cohere retrieval models |
| DeepInfra | `deepinfra` | OpenAI chat completions (preferred) | Open models per token, and your own weights on dedicated GPUs |
| DeepSeek | `deepseek` | OpenAI chat completions (preferred) | DeepSeek V4 and the reasoning line |
| DigitalOcean Gradient | `digitalocean` | OpenAI chat completions (preferred) | Open models on DigitalOcean serverless inference |
| Fireworks AI | `fireworks` | OpenAI chat completions (preferred) | Open models and your own fine-tunes on dedicated GPUs |
| Google Gemini | `google` | Gemini generateContent (preferred) | Gemini Pro and Flash on the Gemini Developer API |
| Groq | `groq` | OpenAI chat completions (preferred) | Open models at very low latency |
| iFlytek Spark | `spark` | OpenAI chat completions (preferred) | Spark X2, X1.5 and the 4.0 Ultra line |
| MiniMax | `minimax` | OpenAI chat completions (preferred) | MiniMax M3 and the M2 family |
| Mistral AI | `mistral` | OpenAI chat completions (preferred) | Mistral Large, Medium and Codestral |
| Modal | `modal` | OpenAI chat completions (preferred) | Your own endpoints on Modal |
| Moonshot (Kimi) | `moonshot` | OpenAI chat completions (preferred) | Kimi K3 and the K2.7 coding models |
| Nebius Token Factory | `nebius` | OpenAI chat completions (preferred) | Open models, and LoRA adapters on curated bases |
| Novita | `novita` | OpenAI chat completions (preferred) | Open models, and any Hugging Face repo on a dedicated endpoint |
| OpenAI | `openai` | OpenAI chat completions (preferred) | GPT-5 and the o-series |
| OpenRouter | `openrouter` | OpenAI chat completions (preferred) | One key across hundreds of models and providers |
| Qwen (QwenCloud) | `qwen` | OpenAI chat completions (preferred) | Qwen3 Max, Plus and Flash on QwenCloud |
| RunPod | `runpod` | OpenAI chat completions (preferred) | Your own serverless vLLM worker |
| SambaNova | `sambanova` | OpenAI chat completions (preferred) | Open models on RDU hardware |
| Straitly | `straitly` | OpenAI chat completions (preferred) | One key across 177 models, with cashback instead of a markup |
| Tencent Hunyuan (TokenHub) | `hunyuan` | OpenAI chat completions (preferred) | Hunyuan, plus DeepSeek, GLM, Kimi and MiniMax on TokenHub |
| Together AI | `together` | OpenAI chat completions (preferred) | A large open-model catalog, and your own fine-tunes |
| Upstage Solar | `upstage` | OpenAI chat completions (preferred) | Solar Pro 4 and Solar Mini |
| Writer (Palmyra) | `writer` | OpenAI chat completions (preferred) | Palmyra X6, X5 and the domain models |
| xAI (Grok) | `xai` | OpenAI chat completions (preferred) | Grok 4 and the Grok code models |
| Z.ai (GLM) | `zai` | OpenAI chat completions (preferred) | GLM 5.3 and the GLM coding models |

{/* generated:providers end */}

Each native type ships with the right default base URL; set `configuration.apiUrl` to point at a compatible proxy instead. For `custom`, set `configuration.apiUrl` to the provider's OpenAI-compatible endpoint. The `configuration.apiKey` field accepts whatever token the provider expects.

### Types not in the table above

Four more provider types ship and are selectable in the create form, but have no row above: the table is generated from the provider registry, and these four are still served by the older per-vendor code path. Nothing about configuring them differs. The [vendor reference](/models/providers) carries base URL, auth and model-listing detail for each.

| Type | Vendor | Notes |
|------|--------|-------|
| `azure_openai` | Azure OpenAI | Base resolves from your resource name; authenticates with an `api-key` header. Not the same product as `azure_ai_foundry`, which does have a row above. |
| `vertex_ai` | Google Vertex AI | Authenticates with a short-lived OAuth token minted per call from a service account. Not the same product as `google`, the Gemini Developer API. |
| `huggingface` | Hugging Face | Bearer `hf_...`; the model list reports per-provider tool support and pricing. |
| `perplexity` | Perplexity | Bearer key on a Responses-shaped surface, with an unauthenticated model list. |

### Local models with Ollama

The `ollama` type talks to an [Ollama](https://ollama.com) server. Chat, streaming, and tool calling ride Ollama's OpenAI-compatible `/v1` endpoint; the model dropdown is populated from the native `GET /api/tags` (your locally pulled models); memory embeddings use the native `POST /api/embed`. No API key is required. If you front Ollama with an authenticating reverse proxy, configure a key and it is sent as a Bearer token.

Because Ollama usually listens on localhost or a private network, almyty's SSRF protection blocks such URLs by default. Self-hosted deployments opt in with `OLLAMA_ALLOW_PRIVATE_URLS=true` on the backend (see [Self-hosting](/self-hosting#local-models-with-ollama)). On hosted almyty the Ollama server must be reachable at a public URL.

### Ollama cloud

The same provider type also works with [Ollama's hosted service](https://ollama.com): set the base URL to `https://ollama.com` and paste an API key from [ollama.com/settings/keys](https://ollama.com/settings/keys). You get the hosted model catalog (large open models like `gpt-oss:120b` or `qwen3.5:397b` without local hardware), the key is sent as a Bearer token, and no SSRF opt-in is needed (it is a public endpoint like any other provider). Hosted-model usage is billed by Ollama; almyty's cost accounting treats the type as zero-cost, so track spend in your Ollama account.

## Configuration reference

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | string | yes | Display name, up to 100 characters |
| `type` | enum | yes | One of the provider types listed above |
| `configuration` | object | yes | Everything below nests inside it |
| `description` | string | no | Up to 1000 characters |
| `configuration.apiKey` | string | no | Provider key, encrypted at rest. Omitted for `ollama` |
| `configuration.apiUrl` | string | no | Overrides the type's default base URL; the field a compatible proxy goes in. Required for `custom` |
| `configuration.model` | string | no | Default model. Left blank, it resolves to the vendor's current default by family rather than a hardcoded id |
| `configuration.maxTokens` | number | no | Sent when set; otherwise the vendor's own default applies |
| `configuration.temperature` | number | no | Sent when set; otherwise the vendor's own default applies |
| `configuration.topP` | number | no | Sent when set; otherwise the vendor's own default applies |

There is no top-level `baseUrl`, `apiKey` or `model` field: all three nest under `configuration`, and the base-URL override is named `apiUrl`. See the [LLM Providers API](/api-reference/llm-providers-api) for the full body.

## Cost tracking

almyty tracks token usage and estimated cost for every LLM call. View aggregate costs per provider in the **Analytics** dashboard or on the provider detail page.

Estimates use built-in list-price tables per provider type (OpenAI, Anthropic, Gemini, Mistral including Codestral and `mistral-embed`, xAI, DeepSeek, Groq, Together, Cohere, and OpenRouter passthrough), refreshed against published prices as of January 2026. Ollama usage is always recorded at $0: local inference has no per-token price. Configure per-model pricing on a provider to override the defaults. See [Cost governance](/cost-governance) for reconciliation against provider actuals.

## Memory embeddings

The [Memory](/memory) module embeds content with the organization's own providers. When several are configured, OpenAI (`text-embedding-3-small`) is preferred over Mistral (`mistral-embed`), then Ollama (`nomic-embed-text` by default via the native `/api/embed` endpoint, overridable with `configuration.embeddingModel`); with none, a deterministic local fallback keeps memory searchable. Each stored memory records the model that embedded it (dimensions vary per model), and vector search only compares like with like.

## Usage in agents

When building an agent pipeline, Model Call nodes reference a provider by ID. The provider's default model and parameters are used unless overridden in the node configuration. See [Agents](/agents) for details.

## API

Provider CRUD, connection tests, and chat sessions are available over REST. See the [AI Models API reference](/api-reference/llm-providers-api).
