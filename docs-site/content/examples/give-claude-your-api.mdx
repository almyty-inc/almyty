# Give Claude Code your own API

**Stack**: `Trigger: Claude Code / any MCP client` · `Tools: your API (any schema)` · `Gateway: MCP (+ A2A, UTCP)` · `Auth: x-api-key` · `Output: Claude calls your API`

**Who it's for**: anyone who wants Claude Code, Cursor, or Claude Desktop to call an API they already run.

This recipe is **real**: the *Weather MCP* gateway below is live in the demo org, serving a
public weather API as tools over MCP that Claude can call directly.

## Build it

1. **Connect the API.** In *APIs → Connect API*, paste a schema URL, OpenAPI, GraphQL, SOAP/WSDL,
   or Protobuf. almyty generates a typed tool for every operation. (The demo imported a public
   weather API and turned its forecast operations into tools.)

   ![A weather API imported, its operations turned into tools](/screenshots/quickstart-api-imported.png)

2. **Publish an MCP gateway.** Group the tools to expose and publish them as an **MCP** gateway,
   then generate an `x-api-key` on its Authentication panel. Scope exactly which tools it serves.

   ![The weather MCP gateway, endpoint, API keys, and tool scoping](/screenshots/gateway-weather-config.png)

3. **Grab the endpoint.** The gateway's **Integrations** tab gives you the live MCP endpoint
   (`https://api.almyty.com/{org}/{gateway}`), plus SSE and discovery URLs:

   ![The gateway's MCP endpoint, SSE, and discovery URLs](/screenshots/gateway-weather-integration.png)

4. **Add it to your client** and use it:
   ```bash
   claude mcp add weather --transport http https://api.almyty.com/northwind-ai/weather
   # clients send: x-api-key: <your gateway key>
   ```
   ```text
   > what's the 3-day forecast for Berlin?
     Claude calls getForecast via almyty → clear, 18°C rising to 24°C
   ```

## Make it yours

- The same gateway also speaks **A2A** and **UTCP**, so other agents and clients reach identical tools.
- Rotate or revoke keys per client from the Authentication panel, one key per client.
- Front a [legacy SOAP service](/examples/modernize-legacy-soap) the same way.

**Uses**: `Platform` · `MCP` · `Claude Code`

## Related

- [MCP gateway](/gateways/mcp) · [MCP server CLI](/cli/mcp-server) · [Connect an API](/tools)
