// Models section: what a card is, then how the router uses it, then where
// models come from (vendors, your own deployments) and the terminal.
export default {
  index: 'Overview',
  routing: 'Routing',
  providers: 'Providers',
  deployments: 'Deployments',
  cli: 'CLI',
}
