// Models section: connecting providers and the model list, then how the
// router uses it, then the provider reference, your cloud account and the
// terminal.
export default {
  index: 'Overview',
  routing: 'Routing',
  providers: 'Providers',
  'your-cloud': 'Your Cloud Account',
  cli: 'CLI',
}
