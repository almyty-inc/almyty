// Models section: the list and what a model records, then how the router
// uses it, then where models run (inference providers, your cloud account)
// and the terminal.
export default {
  index: 'Overview',
  routing: 'Routing',
  providers: 'Inference Providers',
  'your-cloud': 'Your Cloud Account',
  cli: 'CLI',
}
