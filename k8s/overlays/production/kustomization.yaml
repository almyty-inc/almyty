apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: almyty-production

namePrefix: prod-

resources:
  - ../../base
  # secret.yaml removed — secrets managed via kubectl in CI/CD workflow

patches:
  - target:
      kind: Namespace
      name: almyty
    patch: |-
      - op: replace
        path: /metadata/name
        value: almyty-production

  - target:
      kind: ConfigMap
      name: almyty-config
    patch: |-
      - op: replace
        path: /data/NODE_ENV
        value: production
      - op: replace
        path: /data/SWAGGER_ENABLED
        value: "false"
      - op: replace
        path: /data/LOG_LEVEL
        value: warn
      - op: replace
        path: /data/CORS_ORIGIN
        value: "https://app.almyty.dev"
      - op: replace
        path: /data/DATABASE_HOST
        value: "prod-almyty-postgres"
      - op: replace
        path: /data/REDIS_HOST
        value: "prod-almyty-redis"
      - op: replace
        path: /data/DB_SSL
        value: "true"

  - target:
      kind: Deployment
      name: api
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3

  - target:
      kind: Deployment
      name: frontend
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2

  # Production uses managed database — scale in-cluster postgres to 0
  - target:
      kind: Deployment
      name: postgres
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 0

  - target:
      kind: Ingress
      name: almyty-ingress
    patch: |-
      - op: replace
        path: /metadata/annotations/cert-manager.io~1cluster-issuer
        value: prod-letsencrypt-prod
      - op: replace
        path: /spec/tls/0/hosts/0
        value: api.almyty.dev
      - op: replace
        path: /spec/tls/1/hosts/0
        value: app.almyty.dev
      - op: replace
        path: /spec/rules/0/host
        value: api.almyty.dev
      - op: replace
        path: /spec/rules/1/host
        value: app.almyty.dev

images:
  - name: ghcr.io/frane/almyty/api
    newTag: v1.0.0
  - name: ghcr.io/frane/almyty/frontend
    newTag: v1.0.0
