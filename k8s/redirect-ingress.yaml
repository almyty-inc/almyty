# Redirect ingress for alternate domains → almyty.dev
# Applied standalone (not via kustomize) to almyty-staging namespace.
# DNS A records for these domains point to the same LB IP as almyty.dev.
#
# Note: Ingress wildcards only match ONE subdomain level.
# *.almyty.ai matches foo.almyty.ai but NOT foo.bar.almyty.ai.
# So we add explicit *.staging. and *.dev. rules for multi-level subdomains.
#
# TLS certs issued for bare domains only (almyty.ai, almyty.com).
# Wildcard subdomains redirect on HTTP only (no cert = HTTPS cert warning).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: almyty-redirect
  namespace: almyty-staging
  annotations:
    nginx.ingress.kubernetes.io/permanent-redirect: "https://almyty.dev"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: staging-letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - almyty.ai
    secretName: almyty-ai-redirect-tls
  - hosts:
    - almyty.com
    secretName: almyty-com-redirect-tls
  rules:
  # Bare domains
  - host: almyty.ai
    http: &redirect
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: staging-almyty-frontend
            port:
              number: 8080
  - host: almyty.com
    http: *redirect
  # Single-level wildcards (foo.almyty.ai)
  - host: "*.almyty.ai"
    http: *redirect
  - host: "*.almyty.com"
    http: *redirect
  # Multi-level: *.staging.almyty.ai (app.staging.almyty.ai)
  - host: "*.staging.almyty.ai"
    http: *redirect
  - host: "*.staging.almyty.com"
    http: *redirect
  # Multi-level: *.dev.almyty.ai (api.dev.almyty.ai)
  - host: "*.dev.almyty.ai"
    http: *redirect
  - host: "*.dev.almyty.com"
    http: *redirect
