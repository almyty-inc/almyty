Command from almyty-frontend. Use via `window.AlmytyDS.Command` (bundle loaded from the root `_ds_bundle.js`).

## Related

`CommandDialog`, `CommandEmpty`, `CommandGroup`, `CommandInput`, `CommandItem`, `CommandList`
