import * as React from 'react';

/**
 * AlertDialogFooter — from almyty-frontend@1.0.0.
 */
export interface AlertDialogFooterProps {
  [key: string]: unknown;
}

export declare const AlertDialogFooter: React.ComponentType<AlertDialogFooterProps>;
