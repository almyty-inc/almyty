Sheet from almyty-frontend. Use via `window.AlmytyDS.Sheet` (bundle loaded from the root `_ds_bundle.js`).

## Related

`SheetClose`, `SheetContent`, `SheetDescription`, `SheetFooter`, `SheetHeader`, `SheetOverlay`, `SheetPortal`, `SheetTitle`, `SheetTrigger`
