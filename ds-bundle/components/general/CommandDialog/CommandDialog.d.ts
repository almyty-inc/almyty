import * as React from 'react';

/**
 * CommandDialog — from almyty-frontend@1.0.0.
 */
export interface CommandDialogProps {
  [key: string]: unknown;
}

export declare const CommandDialog: React.ComponentType<CommandDialogProps>;
